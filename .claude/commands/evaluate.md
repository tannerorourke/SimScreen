Evaluate the applicant against a parsed JD using the `application-evaluator` subagent.

Target: $ARGUMENTS

Prerequisites: the company profile, the JD parse, and the applicant profile (`application/profile.md`) must all exist. If any are missing, propose running the missing prerequisites in dependency order and await user confirmation per the standard flow.
