Parse a job description using the `jd-parser` subagent.

Target: $ARGUMENTS (file path or URL)

Prerequisite: the corresponding `companies/[name]/[name].md` must exist. If missing, propose running `company-researcher` first and await user confirmation per the standard flow.
