Build or update the applicant profile using the `applicant-analyzer` subagent.

Target: $ARGUMENTS (optional - specific materials to focus on; empty triggers a full scan)

Init mode if `application/profile.md` does not exist; update mode otherwise. Deep research into discovered threads is expected - drill into URLs, papers, and repos surfaced in the materials.
