Research a company using the `company-researcher` subagent.

Target: $ARGUMENTS

Use the agent's deep-search methodology: inventory the obvious sources, identify threads worth drilling into, fetch primary sources, and synthesize with citations. Follow the standard confirmation flow if anything else needs to happen first.
